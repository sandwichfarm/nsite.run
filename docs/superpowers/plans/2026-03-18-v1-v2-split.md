# V1/V2 Protocol Split Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add V1/V2 protocol version tagging to Tools & Resources, with a toggle, sorted/opacity behavior, and a "How to contribute" scroll nudge on the hero.

**Architecture:** Version tags are added to `tools-resources.yaml` items. `ToolsResources.svelte` gains a toggle and sorting/opacity logic. `App.svelte` gets a scroll nudge and an `id` anchor on the Tools section.

**Tech Stack:** Svelte 4, Tailwind CSS 3, YAML (via @rollup/plugin-yaml), CSS animations

---

### Task 1: Add version tags to YAML data

**Files:**
- Modify: `apps/spa/src/lib/tools-resources.yaml`

- [ ] **Step 1: Add `versions` field to all versioned items**

Add `versions: [v1]` or `versions: [v2]` to every item in Gateways, Deploy Tools, Management Tools, and Relays categories. Leave Blossom Servers, Reference, and Informational items untouched (version-agnostic).

```yaml
categories:
  - name: "Gateways"
    items:
      - name: "nsite.run"
        url: "https://nsite.run"
        description: "Cloudflare Workers gateway with integrated relay and blossom server"
        versions: [v1]
      - name: "nosto.re"
        url: "https://nosto.re"
        description: "Public nsite gateway by nosto.re"
        versions: [v1]
      - name: "nwb.tf"
        url: "https://nwb.tf/"
        description: "Public nsite gateway"
        versions: [v1]
      - name: "nsite-gateway"
        url: "https://github.com/hzrd149/nsite-gateway"
        description: "TypeScript gateway for static websites — self-hostable"
        versions: [v2]
      - name: "nostr-deploy-server"
        url: "https://github.com/sepehr-safari/nostr-deploy-server"
        description: "Node.js server implementation for serving nsites"
        versions: [v1]

  - name: "Deploy Tools"
    items:
      - name: "nsite-cli"
        url: "https://github.com/flox1an/nsite-cli"
        description: "CLI deployment tool by flox1an — supports private key signing"
        versions: [v1]
      - name: "nsyte"
        url: "https://github.com/sandwichfarm/nsyte"
        description: "CLI deployment tool with bunker and private key support"
        versions: [v2]
      - name: "nous-cli"
        url: "https://gitlab.com/soapbox-pub/nous-cli"
        description: "CLI deployment tool by Soapbox"
        versions: [v1]
      - name: "nostr-deploy-cli"
        url: "https://github.com/sepehr-safari/nostr-deploy-cli"
        description: "CLI deployment tool by sepehr-safari"
        versions: [v1]
      - name: "nsite-action"
        url: "https://github.com/marketplace/actions/nsite-action"
        description: "GitHub Action for automated nsite deployments on push"
        versions: [v1]

  - name: "Management Tools"
    items:
      - name: "nsite-manager"
        url: "https://github.com/hzrd149/nsite-manager"
        description: "Web-based management dashboard for your nsites"
        versions: [v1]

  - name: "Blossom Servers"
    items:
      - name: "Find Blossom Servers"
        url: "https://blossomservers.com"
        description: "Directory of publicly available Blossom servers"

  - name: "Relays"
    items:
      - name: "relay.nsite.lol"
        url: "https://relay.nsite.lol"
        description: "Relay optimized for nsite manifests"
        versions: [v1]
      - name: "relay.nosto.re"
        url: "https://relay.nosto.re"
        description: "Relay optimized for nsite manifests by nosto.re"
        versions: [v1]

  - name: "Reference"
    items:
      - name: "nostr Protocol (NIPs)"
        url: "https://github.com/nostr-protocol/nips"
        description: "nostr implementation possibilities — the protocol specification"
      - name: "Blossom Specification"
        url: "https://github.com/hzrd149/blossom"
        description: "HTTP endpoints for storing blobs on publicly accessible servers"
      - name: "NIP Draft — nsite by Lez"
        url: "https://github.com/lez/nsite"
        description: "Original nsite NIP draft by Lez"
      - name: "NIP PR #1538 — nsite by hzrd149"
        url: "https://github.com/nostr-protocol/nips/pull/1538"
        description: "Open PR proposing nsite support in the nostr protocol"

  - name: "Informational"
    items:
      - name: "awesome-nsite"
        url: "https://github.com/nostrver-se/awesome-nsite"
        description: "A curated list of resources related to nsites"
```

- [ ] **Step 2: Verify the SPA still builds**

Run: `cd apps/spa && npx vite build`
Expected: Build completes without errors.

- [ ] **Step 3: Commit**

```bash
git add apps/spa/src/lib/tools-resources.yaml
git commit -m "feat: add v1/v2 protocol version tags to tools-resources.yaml"
```

---

### Task 2: Add V1/V2 toggle and sorting/opacity to ToolsResources.svelte

**Files:**
- Modify: `apps/spa/src/components/ToolsResources.svelte`

- [ ] **Step 1: Rewrite ToolsResources.svelte with toggle, sorting, and opacity logic**

Replace the entire file content with:

```svelte
<script>
  import { onMount } from 'svelte';
  import toolsData from '../lib/tools-resources.yaml';

  let selectedVersion = 'v2';
  let headingVisible = true;
  let headingEl;

  onMount(() => {
    if (!headingEl) return;
    const observer = new IntersectionObserver(
      ([entry]) => { headingVisible = entry.isIntersecting; },
      { threshold: 0 }
    );
    observer.observe(headingEl);
    return () => observer.disconnect();
  });

  function sortedItems(items) {
    const matching = [];
    const nonMatching = [];
    for (const item of items) {
      if (!item.versions) {
        matching.push(item); // version-agnostic always "matches"
      } else if (item.versions.includes(selectedVersion)) {
        matching.push(item);
      } else {
        nonMatching.push(item);
      }
    }
    return [...matching, ...nonMatching];
  }

  function itemMatches(item) {
    if (!item.versions) return true;
    return item.versions.includes(selectedVersion);
  }
</script>

<div>
  <!-- Section heading (observed for sticky behavior) -->
  <h2 bind:this={headingEl} class="text-2xl font-semibold text-white mb-6">Tools &amp; Resources</h2>

  <!-- Sticky toggle bar -->
  <div
    class="flex gap-2 justify-center mb-8 py-3 z-10 transition-all duration-200
      {headingVisible ? '' : 'sticky top-0 bg-slate-900/95 backdrop-blur rounded-b-lg'}"
  >
    <button
      on:click={() => (selectedVersion = 'v1')}
      class="px-8 py-3 rounded-lg text-base font-semibold transition-all duration-200
        {selectedVersion === 'v1'
          ? 'bg-purple-600 text-white shadow-[0_0_20px_rgba(124,58,237,0.3)]'
          : 'bg-slate-800 text-slate-400 border border-slate-600 hover:bg-slate-700 hover:text-slate-300'}"
    >
      nsite v1
    </button>
    <button
      on:click={() => (selectedVersion = 'v2')}
      class="px-8 py-3 rounded-lg text-base font-semibold transition-all duration-200
        {selectedVersion === 'v2'
          ? 'bg-purple-600 text-white shadow-[0_0_20px_rgba(124,58,237,0.3)]'
          : 'bg-slate-800 text-slate-400 border border-slate-600 hover:bg-slate-700 hover:text-slate-300'}"
    >
      nsite v2
    </button>
  </div>

  <!-- Category listings -->
  <div class="space-y-8">
    {#each toolsData.categories as category}
      <div>
        <h3 class="text-lg font-semibold text-white mb-3">{category.name}</h3>
        <div class="grid grid-cols-1 sm:grid-cols-2 gap-3">
          {#each sortedItems(category.items) as item (item.name)}
            {@const matches = itemMatches(item)}
            {#if matches}
              <a
                href={item.url}
                target="_blank"
                rel="noopener noreferrer"
                class="block bg-slate-800 rounded-lg p-4 hover:bg-slate-700 transition-all duration-300 group"
              >
                <p class="font-medium text-purple-400 group-hover:text-purple-300 transition-colors text-sm">
                  {item.name}
                </p>
                <p class="text-slate-400 text-xs mt-1 leading-relaxed">{item.description}</p>
              </a>
            {:else}
              <div
                class="block bg-slate-800 rounded-lg p-4 opacity-35 cursor-default transition-all duration-300"
              >
                <p class="font-medium text-purple-400 text-sm">
                  {item.name}
                </p>
                <p class="text-slate-400 text-xs mt-1 leading-relaxed">{item.description}</p>
              </div>
            {/if}
          {/each}
        </div>
      </div>
    {/each}
  </div>
</div>
```

Key details:
- `sortedItems()` does a stable partition: matching items first, non-matching after, preserving YAML order within each group.
- `itemMatches()` returns `true` for version-agnostic items (no `versions` field).
- Non-matching items render as `<div>` instead of `<a>` — no `href`, no `pointer-events`, just a faded card.
- Svelte keyed each (`(item.name)`) ensures smooth DOM reordering on toggle.
- Sticky behavior uses Intersection Observer on the heading element, toggling a `sticky top-0` class.

- [ ] **Step 2: Verify the SPA builds**

Run: `cd apps/spa && npx vite build`
Expected: Build completes without errors.

- [ ] **Step 3: Start dev server and visually verify**

Run: `cd apps/spa && npx vite dev`

Verify:
- V2 is selected by default
- nsite-gateway appears first in Gateways, nsyte first in Deploy Tools
- All V1-only items are faded (opacity 0.35) and not clickable
- Clicking "nsite v1" flips: V1 items become full opacity, V2 items fade
- Reference, Informational, and Blossom Servers items are always full opacity
- Toggle becomes sticky when scrolling past the heading
- Transitions are smooth (0.3s)

- [ ] **Step 4: Commit**

```bash
git add apps/spa/src/components/ToolsResources.svelte
git commit -m "feat: add v1/v2 toggle with sorting and opacity to ToolsResources"
```

---

### Task 3: Update App.svelte — remove duplicated heading, add scroll anchor and nudge

**Files:**
- Modify: `apps/spa/src/App.svelte`

- [ ] **Step 1: Add `id` to Tools & Resources section and remove the old `<h2>`**

The `<h2>Tools & Resources</h2>` heading is now rendered inside `ToolsResources.svelte` (Task 2). Remove it from `App.svelte` and add an `id="tools"` to the wrapping `<section>` for scroll targeting.

Find this block in `App.svelte` (around line 441-445):

```svelte
        <!-- Tools & Resources -->
        <section>
          <h2 class="text-2xl font-semibold text-white mb-6">Tools &amp; Resources</h2>
          <ToolsResources />
        </section>
```

Replace with:

```svelte
        <!-- Tools & Resources -->
        <section id="tools">
          <ToolsResources />
        </section>
```

- [ ] **Step 2: Add "How to contribute" scroll nudge to the hero section**

Find the closing `</section>` of the hero (after the login/identity block, around line 387). Insert the scroll nudge just before it.

Find:

```svelte
        {/if}
      </section>
```

(This is the `{/if}` closing the `$session.pubkey` conditional, followed by `</section>` closing the hero.)

Replace with:

```svelte
        {/if}

        <!-- Scroll nudge -->
        <button
          on:click={() => document.getElementById('tools')?.scrollIntoView({ behavior: 'smooth' })}
          class="mt-10 flex flex-col items-center gap-1 text-purple-400 hover:text-purple-300 transition-colors animate-bounce-subtle"
        >
          <span class="text-sm">How to contribute</span>
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
          </svg>
        </button>
      </section>
```

- [ ] **Step 3: Add the bounce animation CSS**

Find the `app.css` file at `apps/spa/src/app.css` and add the custom animation keyframes. The file currently only contains Tailwind directives. Add after the existing content:

```css
@keyframes bounce-subtle {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(6px); }
}

.animate-bounce-subtle {
  animation: bounce-subtle 2s ease-in-out infinite;
}
```

- [ ] **Step 4: Verify the SPA builds**

Run: `cd apps/spa && npx vite build`
Expected: Build completes without errors.

- [ ] **Step 5: Start dev server and visually verify**

Run: `cd apps/spa && npx vite dev`

Verify:
- "How to contribute" text with animated bouncing arrow appears at the bottom of the hero section
- Clicking it smooth-scrolls to the Tools & Resources section
- The nudge scrolls away naturally as the user scrolls down
- The Tools & Resources section heading still displays correctly (from ToolsResources.svelte)

- [ ] **Step 6: Commit**

```bash
git add apps/spa/src/App.svelte apps/spa/src/app.css
git commit -m "feat: add 'How to contribute' scroll nudge and tools section anchor"
```
