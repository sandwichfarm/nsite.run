---
phase: 03-blossom
plan: 01
type: execute
wave: 1
depends_on: ["03-00"]
files_modified:
  - apps/blossom/deno.json
  - apps/blossom/src/types.ts
  - apps/blossom/src/util.ts
  - apps/blossom/src/storage/client.ts
  - apps/blossom/src/storage/metadata.ts
  - apps/blossom/src/auth/nostr.ts
  - apps/blossom/src/middleware/cors.ts
autonomous: true
requirements: [BLSM-02, BLSM-06, BLSM-07]

must_haves:
  truths:
    - "Blossom types are defined and importable by handler code"
    - "StorageClient can perform CRUD against Bunny Storage REST API"
    - "Metadata layer manages blob ownership, indexes, blocked hashes, and reports"
    - "Auth validates kind 24242 events with 120s window, required x tag for upload, nostr-tools verification"
    - "CORS middleware produces BUD-compliant headers on all responses"
  artifacts:
    - path: "apps/blossom/src/types.ts"
      provides: "BlobDescriptor, BlobMeta, BlobIndexEntry, Config, AuthResult, StoredReport, BlockedConfig"
      contains: "interface BlobDescriptor"
    - path: "apps/blossom/src/util.ts"
      provides: "fromBase64, isValidSha256, isValidPubkey, jsonResponse, errorResponse"
      contains: "function errorResponse"
    - path: "apps/blossom/src/storage/client.ts"
      provides: "StorageClient class with put/get/head/delete/getJson/putJson/getToml/blobPath/blobUrl/metaPath/listPath/reportPath"
      contains: "class StorageClient"
    - path: "apps/blossom/src/storage/metadata.ts"
      provides: "getMeta, putMeta, addOwner, removeOwner, getIndex, addToIndex, removeFromIndex, getReports, addReport, isBlocked"
      contains: "function addOwner"
    - path: "apps/blossom/src/auth/nostr.ts"
      provides: "validateAuth function using nostr-tools getEventHash/verifyEvent"
      contains: "function validateAuth"
    - path: "apps/blossom/src/middleware/cors.ts"
      provides: "handleOptions, withCors"
      contains: "function withCors"
  key_links:
    - from: "apps/blossom/src/auth/nostr.ts"
      to: "@nostr/tools/pure"
      via: "import getEventHash, verifyEvent"
      pattern: "getEventHash|verifyEvent"
    - from: "apps/blossom/src/storage/client.ts"
      to: "Bunny Storage REST API"
      via: "fetch() calls to storage hostname"
      pattern: "fetch.*baseUrl"
    - from: "apps/blossom/src/storage/metadata.ts"
      to: "apps/blossom/src/storage/client.ts"
      via: "StorageClient methods for JSON/TOML reads"
      pattern: "storage\\.getJson|storage\\.putJson|storage\\.getToml"
---

<objective>
Port blssm.us foundation modules (types, util, storage, auth, cors) into apps/blossom/src with targeted adaptations: strip payment/access types, switch schnorr from @noble/curves to nostr-tools, adjust auth window to 120s, import NostrEvent from @nsite/shared.

Purpose: Establish all dependencies that blossom handlers need before any handler code can be written.
Output: 7 files -- the complete foundation layer for the blossom server.
</objective>

<execution_context>
@/home/sandwich/.claude/get-shit-done/workflows/execute-plan.md
@/home/sandwich/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-blossom/03-CONTEXT.md
@.planning/phases/03-blossom/03-RESEARCH.md
@.planning/phases/03-blossom/03-00-SUMMARY.md

Source reference for porting: ~/Develop/blssm.us/src/

<interfaces>
<!-- Key types and contracts from @nsite/shared that blossom will import -->

From packages/shared/src/types.ts:
```typescript
export interface NostrEvent {
  id: string;
  pubkey: string;
  created_at: number;
  kind: number;
  tags: string[][];
  content: string;
  sig: string;
}
```

From packages/shared/src/constants.ts:
```typescript
export const ALLOWED_KINDS: readonly number[] = [5, 15128, 35128, 10002, 10063];
```

From packages/shared/src/sha256.ts:
```typescript
export async function sha256Hex(data: Uint8Array): Promise<string>;
// NOTE: This is ASYNC (WebCrypto). blssm.us sha256Hex is sync (@noble/hashes).
// All call sites in blossom must use: const hash = await sha256Hex(data);
```

From @nostr/tools/pure (replaces blssm.us schnorr.ts):
```typescript
export function getEventHash(event: UnsignedEvent): string;
export function verifyEvent(event: Event): boolean;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dependencies and create types + util modules</name>
  <files>apps/blossom/deno.json, apps/blossom/src/types.ts, apps/blossom/src/util.ts, apps/blossom/src/middleware/cors.ts</files>
  <action>
1. **Update apps/blossom/deno.json** -- add imports:
   ```json
   "imports": {
     "@nostr/tools/pure": "jsr:@nostr/tools@^2.23.3/pure",
     "@std/toml": "jsr:@std/toml@^1.0.0"
   }
   ```

2. **Create apps/blossom/src/types.ts** -- port from ~/Develop/blssm.us/src/types.ts with these changes:
   - Keep: BlobDescriptor, BlobMeta, BlobIndexEntry, StoredReport, BlockedConfig, AuthResult
   - Import NostrEvent from "@nsite/shared/types" and re-export it (do NOT define a local NostrEvent)
   - Replace Config: strip spaStoragePassword/spaStorageHostname/spaStorageUsername fields. Keep only: storagePassword, storageHostname, storageUsername, cdnHostname, serverUrl, maxUploadSize (number, default 104857600)
   - Delete: AccessConfig, PaymentConfig, PricingConfig, LightningConfig, CacheConfig, PaymentInfo, MintEntry, PaymentAmounts, ServerInfo, ValidationResult (not needed -- open access, no payments)

3. **Create apps/blossom/src/util.ts** -- port from ~/Develop/blssm.us/src/util.ts with these changes:
   - Remove: sha256Hex function entirely (use @nsite/shared/sha256 instead), bytesToHex, hexToBytes (not needed outside sha256), toBase64 (not needed)
   - Keep: fromBase64, isValidSha256, isValidPubkey, prefix, jsonResponse, errorResponse
   - Remove the @noble/hashes import entirely

4. **Create apps/blossom/src/middleware/cors.ts** -- port from ~/Develop/blssm.us/src/middleware/cors.ts exactly as-is. No changes needed. Contains handleOptions() and withCors().
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nsite.run && deno check apps/blossom/src/types.ts apps/blossom/src/util.ts apps/blossom/src/middleware/cors.ts</automated>
  </verify>
  <done>deno.json has @nostr/tools/pure and @std/toml imports; types.ts exports BlobDescriptor/BlobMeta/BlobIndexEntry/Config/AuthResult/StoredReport/BlockedConfig with NostrEvent re-exported from @nsite/shared; util.ts exports fromBase64/isValidSha256/isValidPubkey/prefix/jsonResponse/errorResponse; cors.ts exports handleOptions/withCors</done>
</task>

<task type="auto">
  <name>Task 2: Port storage client and metadata modules</name>
  <files>apps/blossom/src/storage/client.ts, apps/blossom/src/storage/metadata.ts</files>
  <action>
1. **Create apps/blossom/src/storage/client.ts** -- port from ~/Develop/blssm.us/src/storage/client.ts with these changes:
   - Import Config from "../types.ts" (not blssm.us types)
   - Import `parse as parseToml` from "@std/toml" (same as blssm.us)
   - Keep ALL methods: put, get, head, delete, getJson, getText, getToml, putJson, blobUrl, blobPath, metaPath, listPath, reportPath, list
   - No functional changes -- this is a direct port with only import path updates
   - Note: put() checks `resp.status === 201` (Bunny Storage returns 201 on success, not 200)

2. **Create apps/blossom/src/storage/metadata.ts** -- port from ~/Develop/blssm.us/src/storage/metadata.ts with these changes:
   - Import types from "../types.ts" (not blssm.us types)
   - Import StorageClient from "./client.ts" (same relative path)
   - Keep ALL functions: getMeta, putMeta, addOwner, removeOwner, getIndex, addToIndex, removeFromIndex, getReports, addReport, getBlocked, isBlocked, _resetBlockedCacheForTesting
   - No functional changes -- direct port with import path updates only
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nsite.run && deno check apps/blossom/src/storage/client.ts apps/blossom/src/storage/metadata.ts && deno test --allow-all apps/blossom/src/storage/client.test.ts</automated>
  </verify>
  <done>StorageClient class exports all Bunny Storage REST methods; metadata.ts exports getMeta/putMeta/addOwner/removeOwner/getIndex/addToIndex/removeFromIndex/getReports/addReport/isBlocked/_resetBlockedCacheForTesting; both pass deno check; storage client tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Port auth module with nostr-tools and 120s window</name>
  <files>apps/blossom/src/auth/nostr.ts</files>
  <action>
1. **Create apps/blossom/src/auth/nostr.ts** -- port from ~/Develop/blssm.us/src/auth/nostr.ts with these changes:
   - Remove import of computeEventId/verifySignature from "./schnorr.ts" (file does not exist in port)
   - Add import: `import { getEventHash, verifyEvent } from "@nostr/tools/pure";`
   - Import NostrEvent and AuthResult from "../types.ts"
   - Import fromBase64 from "../util.ts"
   - Replace `computeEventId(event)` with `getEventHash(event as Parameters<typeof getEventHash>[0])` -- same cast pattern as relay handler.ts
   - Replace `verifySignature(event)` with `verifyEvent(event as Parameters<typeof verifyEvent>[0])`
   - **Change clock skew from 60s to 120s** per locked decision: `if (event.created_at > now + 120)` instead of `+ 60`
   - Keep all other validation: kind 24242 check, expiration tag, t tag verb match, x tag hash match (if present), server tag URL match
   - The x tag enforcement for upload verb is NOT in this module -- it stays in the upload handler. validateAuth only validates x tag IF sha256 option is provided. The locked decision "required x tag" is enforced by the upload handler checking auth.event for x tags after calling validateAuth.
   - Do NOT create schnorr.ts -- nostr-tools replaces it entirely
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nsite.run && deno check apps/blossom/src/auth/nostr.ts && deno test --allow-all apps/blossom/src/auth/nostr.test.ts</automated>
  </verify>
  <done>validateAuth uses getEventHash+verifyEvent from @nostr/tools/pure, 120s clock skew window, no schnorr.ts dependency; passes deno check; auth tests pass</done>
</task>

</tasks>

<verification>
All 7 files pass `deno check`:
```bash
deno check apps/blossom/src/types.ts apps/blossom/src/util.ts apps/blossom/src/middleware/cors.ts apps/blossom/src/storage/client.ts apps/blossom/src/storage/metadata.ts apps/blossom/src/auth/nostr.ts
```

Foundation module tests pass:
```bash
deno test --allow-all apps/blossom/src/auth/nostr.test.ts apps/blossom/src/storage/client.test.ts
```

No payment/access middleware files exist in apps/blossom/src/middleware/ (only cors.ts).
No schnorr.ts exists in apps/blossom/src/auth/ (only nostr.ts).
</verification>

<success_criteria>
- All 7 foundation files exist and pass deno check with zero errors
- types.ts has no payment/access/SPA types
- util.ts has no @noble/hashes import
- auth/nostr.ts uses nostr-tools (not @noble/curves)
- auth/nostr.ts uses 120s clock skew window
- storage/client.ts connects to Bunny Storage via REST
- No middleware/access.ts or middleware/payment-gate.ts exist
- Auth and storage client tests pass via deno test
</success_criteria>

<output>
After completion, create `.planning/phases/03-blossom/03-01-SUMMARY.md`
</output>
