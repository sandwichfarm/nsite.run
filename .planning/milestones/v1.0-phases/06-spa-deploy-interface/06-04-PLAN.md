---
phase: 06-spa-deploy-interface
plan: 04
type: execute
wave: 3
depends_on: ["06-02", "06-03"]
files_modified:
  - apps/spa/src/App.svelte
  - apps/spa/src/components/Navbar.svelte
  - apps/spa/src/components/DeployZone.svelte
  - apps/spa/src/components/FileTree.svelte
  - apps/spa/src/components/ProgressIndicator.svelte
  - apps/spa/src/components/SuccessPanel.svelte
  - apps/spa/src/components/LoginModal.svelte
  - apps/spa/src/components/NIP46Dialog.svelte
  - apps/spa/src/components/AdvancedConfig.svelte
  - apps/spa/src/components/ToolsResources.svelte
  - apps/spa/src/lib/tools-resources.yaml
  - apps/spa/src/lib/__tests__/tools.test.js
autonomous: true
requirements:
  - SPA-01
  - SPA-04
  - SPA-06
  - SPA-11
  - SPA-12

must_haves:
  truths:
    - "Landing page shows deploy zone prominently at top with educational content below"
    - "Drag-drop zone, folder picker button, and archive upload button are all visible"
    - "File tree displays after file selection with expandable directories, sizes, and warning icons"
    - "Flagged files show inline warning with checkbox to exclude/include"
    - "SPA fallback checkbox is present in deploy options"
    - "Progress indicator shows Hashing / Uploading / Publishing / Done steps"
    - "Success panel shows site URL, deploy summary, and share options"
    - "Login modal offers Extension and Remote Signer options"
    - "NIP-46 dialog shows QR code immediately"
    - "Advanced config section is collapsed by default"
    - "Tools & Resources section displays categorized links from YAML data"
    - "Dark theme with slate-900 background and purple/indigo accents"
  artifacts:
    - path: "apps/spa/src/App.svelte"
      provides: "Root component: layout, deploy flow orchestration, educational content"
    - path: "apps/spa/src/components/Navbar.svelte"
      provides: "Top nav with login button, auth status display"
    - path: "apps/spa/src/components/DeployZone.svelte"
      provides: "Drag-drop + folder picker + archive picker"
    - path: "apps/spa/src/components/FileTree.svelte"
      provides: "Expandable file tree with warnings and checkboxes"
    - path: "apps/spa/src/components/ProgressIndicator.svelte"
      provides: "Multi-step deploy progress display"
    - path: "apps/spa/src/components/SuccessPanel.svelte"
      provides: "Post-deploy: site link, summary, share options, nsec display for anonymous"
    - path: "apps/spa/src/components/LoginModal.svelte"
      provides: "Extension vs Remote Signer choice"
    - path: "apps/spa/src/components/NIP46Dialog.svelte"
      provides: "QR code display + bunker URI input"
    - path: "apps/spa/src/components/AdvancedConfig.svelte"
      provides: "Collapsed section for extra relay/blossom URLs"
    - path: "apps/spa/src/components/ToolsResources.svelte"
      provides: "Educational links section from YAML"
    - path: "apps/spa/src/lib/tools-resources.yaml"
      provides: "Categorized links data"
    - path: "apps/spa/src/lib/__tests__/tools.test.js"
      provides: "Tests for tools-resources.yaml structure (SPA-12)"
  key_links:
    - from: "apps/spa/src/App.svelte"
      to: "apps/spa/src/lib/store.js"
      via: "import { session, deployState }"
      pattern: "from.*lib/store"
    - from: "apps/spa/src/App.svelte"
      to: "apps/spa/src/lib/nostr.js"
      via: "import signer creation functions"
      pattern: "from.*lib/nostr"
    - from: "apps/spa/src/components/DeployZone.svelte"
      to: "apps/spa/src/lib/files.js"
      via: "import pickDirectory, pickArchive"
      pattern: "from.*lib/files"
    - from: "apps/spa/src/components/FileTree.svelte"
      to: "apps/spa/src/lib/scanner.js"
      via: "import scanFilename for warning icons"
      pattern: "from.*lib/scanner"
    - from: "apps/spa/src/components/NIP46Dialog.svelte"
      to: "qrcode"
      via: "import QRCode for canvas rendering"
      pattern: "from 'qrcode'"
    - from: "apps/spa/src/components/ToolsResources.svelte"
      to: "apps/spa/src/lib/tools-resources.yaml"
      via: "import toolsData from YAML"
      pattern: "from.*tools-resources.yaml"
---

<objective>
Build all Svelte UI components: the deploy flow (drag-drop, file tree, progress, success), authentication UI (login modal, NIP-46 QR), and educational landing page content.

Purpose: Wire the lib modules from Plans 02 and 03 into a complete visual interface. The user should be able to see and interact with the full deploy flow after this plan.
Output: All 10 Svelte components + tools-resources.yaml data + tools test, wired into App.svelte.
</objective>

<execution_context>
@/home/sandwich/.claude/get-shit-done/workflows/execute-plan.md
@/home/sandwich/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-spa-deploy-interface/06-CONTEXT.md
@.planning/phases/06-spa-deploy-interface/06-RESEARCH.md
@.planning/phases/06-spa-deploy-interface/06-02-SUMMARY.md
@.planning/phases/06-spa-deploy-interface/06-03-SUMMARY.md

<interfaces>
<!-- From Plan 02 (files.js) exports: -->
<!-- pickDirectory() -> [{path, data, size}] -->
<!-- pickArchive() -> [{path, data, size}] -->
<!-- extractZip(arrayBuffer) -> [{path, data, size}] -->
<!-- extractTarGz(arrayBuffer) -> [{path, data, size}] -->
<!-- autoExcludeVCS(files) -> {included, excluded, excludedCount} -->
<!-- buildFileTree(files) -> nested tree {name, path, children?, size?, isDir} -->

<!-- From Plan 02 (scanner.js) exports: -->
<!-- scanFiles(files) -> {warnings: [{path, type, details}], autoExcluded: string[]} -->
<!-- scanFilename(path) -> {dangerous, patterns} -->

<!-- From Plan 02 (crypto.js) exports: -->
<!-- hashFile(arrayBuffer) -> hex string -->

<!-- From Plan 03 (store.js) exports: -->
<!-- session (writable store: {pubkey, signerType, displayName, avatar, npub}) -->
<!-- deployState (writable store: {step, files, warnings, progress, result, error}) -->
<!-- serverConfig (persisted store: {extraRelays, extraBlossoms}) -->

<!-- From Plan 03 (nostr.js) exports: -->
<!-- createAnonymousSigner() -> {signer, pubkey, nsec} -->
<!-- createExtensionSigner() -> {signer, pubkey} -->
<!-- createNostrConnectSigner(relays) -> {signer, uri, waitForSigner} -->
<!-- connectFromBunkerURI(uri) -> {signer, pubkey} -->
<!-- fetchProfile(pubkey, relays) -> {name, display_name, picture} | null -->
<!-- fetchRelayList(pubkey, relays) -> string[] -->
<!-- fetchBlossomList(pubkey, relays) -> string[] -->
<!-- NSITE_RELAY, NSITE_BLOSSOM, DEFAULT_RELAYS -->

<!-- From Plan 03 (upload.js) exports: -->
<!-- uploadAllBlobs(files, signer, blossomUrls, onProgress) -> {uploaded, skipped, failed} -->

<!-- From Plan 03 (publish.js) exports: -->
<!-- publishManifest(signer, files, servers, relays, spaFallback) -> {event, results} -->

<!-- From CONTEXT.md key decisions: -->
<!-- - Single page: deploy prominently at top, educational scrolls below -->
<!-- - Big Deploy button/drop zone visible immediately; small "Login with your identity" link -->
<!-- - Dark theme with dark gray/purple palette, Tailwind CSS -->
<!-- - Anonymous deploy default: generate ephemeral keypair, deploy, show nsec at success -->
<!-- - Login button in top-right navbar -->
<!-- - Two login options: Extension + Remote Signer -->
<!-- - NIP-46 flow: immediately show QR code + bunker URI input -->
<!-- - After login: show avatar + display name + truncated npub -->
<!-- - Expandable "Advanced" section collapsed by default for extra relay/blossom URLs -->
<!-- - File tree shows expandable dirs with sizes, flagged files get warning icons + exclude checkbox -->
<!-- - SPA fallback toggle checkbox -->
<!-- - Progress: Hashing -> Uploading -> Publishing -> Done, each with progress -->
<!-- - Success: link to site, deploy summary, share options (copy URL, share on nostr, view manifest) -->
<!-- - After anonymous deploy: show nsec with copy-to-clipboard -->
<!-- - Port tools-resources.yaml from ~/Develop/nsite.run -->
<!-- - Fresh educational content about nsites, concise -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy flow shell — App.svelte, Navbar, ProgressIndicator, SuccessPanel</name>
  <files>
    apps/spa/src/App.svelte,
    apps/spa/src/components/Navbar.svelte,
    apps/spa/src/components/ProgressIndicator.svelte,
    apps/spa/src/components/SuccessPanel.svelte
  </files>
  <action>
1. **App.svelte** — Root component orchestrating the full deploy flow and landing page:
   - Import all child components, stores (session, deployState, serverConfig), and lib functions.
   - Layout: Navbar at top, then main content area.
   - Deploy flow state machine driven by deployState.step: idle -> selecting -> reviewing -> hashing -> uploading -> publishing -> success | error.
   - **idle state:** Show DeployZone prominently. Below it, educational content sections (What are nsites?, How it works, Tools & Resources).
   - **selecting state:** Show DeployZone in active state.
   - **reviewing state:** Show FileTree with the scanned files + warnings. Below tree: SPA fallback checkbox ("This is a single-page app — serve index.html for all routes"). AdvancedConfig section. "Deploy" button (or "Deploy Anonymously" if not logged in). Show excluded VCS files notice if any ("Excluded N files from .git/").
   - **hashing/uploading/publishing states:** Show ProgressIndicator.
   - **success state:** Show SuccessPanel.
   - **error state:** Show error message with retry button.
   - Deploy handler: On deploy button click —
     a. If no session.pubkey: createAnonymousSigner(), update session store.
     b. Hash all included files (hashFile for each), update progress.
     c. Determine blossom URLs: [NSITE_BLOSSOM, ...serverConfig.extraBlossoms, ...user's kind 10063 list].
     d. Upload all blobs via uploadAllBlobs, update progress.
     e. Determine relay URLs: [NSITE_RELAY, ...serverConfig.extraRelays, ...user's kind 10002 list].
     f. Publish manifest via publishManifest with spaFallback flag.
     g. On success: set deployState to success with event and results.
   - Dark theme: `bg-slate-900 text-gray-100` base, use slate-800 for cards, purple-500/indigo-500 for accents.
   - Educational content sections (in idle state below deploy zone):
     - "What are nsites?" — 2-3 sentences: Nsites are static websites hosted on the Nostr network. Your site is stored as cryptographic blobs on blossom servers and discoverable via nostr events. Anyone can run a gateway to serve nsites.
     - "How it works" — 3 steps: 1) Choose your files, 2) Files are hashed and uploaded to blossom servers, 3) A manifest event is published to relays — gateways find it and serve your site.
     - "Why Nostr?" — Censorship-resistant, self-sovereign identity (your npub owns your site), portable (works across any gateway), no accounts needed.
     - Then ToolsResources component.
     - Tailwind: `max-w-3xl mx-auto`, section spacing with `space-y-16 py-16`.
   - Note: DeployZone, FileTree, LoginModal, NIP46Dialog, AdvancedConfig, ToolsResources are imported but implemented in Tasks 2 and 3. Create placeholder imports — these components will be built in subsequent tasks in this same plan. Use `{#if false}` or conditional rendering to avoid import errors during incremental development — OR create minimal placeholder .svelte files (just `<div>placeholder</div>`) for components not yet built. The build must succeed after this task.

2. **Navbar.svelte** — Top navigation bar:
   - Left: "nsite.run" logo/text link.
   - Right: If not logged in, small "Login with your identity" text button. If logged in, show avatar image (from session.avatar, fallback to identicon/initials), display name, truncated npub (first 8 + last 4 chars). Logout button (clears session store).
   - Clicking login opens LoginModal (dispatch event to App.svelte or use a showLoginModal store).
   - Tailwind: `bg-slate-800 border-b border-slate-700`, sticky top.

3. **ProgressIndicator.svelte** — Multi-step progress display:
   - Props: `step` ('hashing'|'uploading'|'publishing'|'done'), `progress` (0-100 for current step), `details` (string, e.g., "Uploading file 3 of 12").
   - Four steps displayed as horizontal pills/badges: Hashing files -> Uploading blobs -> Publishing manifest -> Done.
   - Current step highlighted (purple), completed steps green with checkmark, future steps gray.
   - Progress bar under current step showing percentage.
   - Details text below.
   - Tailwind: flex horizontal, gap between pills, `bg-slate-800 rounded-lg p-6`.

4. **SuccessPanel.svelte** — Post-deploy success display:
   - Props: `event` (signed manifest), `npub`, `nsec` (if anonymous deploy), `signerType`, `uploadResult`, `publishResult`.
   - Site URL: `https://{npub}.nsite.run` as a clickable link.
   - Deploy summary: number of files, manifest event ID (truncated), servers used.
   - Share options: "Copy URL" button (navigator.clipboard.writeText), "Share on Nostr" button (open web+nostr: intent or Nostr client URL), "View Manifest" button (expandable JSON).
   - If anonymous deploy (signerType === 'anonymous'): prominent nsec display box with "Copy nsec" button. Warning text: "Save this key! You need it to update your site later."
   - Tailwind: `bg-slate-800 rounded-lg p-6`, green accent for success state.
  </action>
  <verify>
    <automated>cd apps/spa && npm run build</automated>
  </verify>
  <done>App.svelte orchestrates the full deploy flow state machine. Navbar shows login state. ProgressIndicator displays multi-step progress. SuccessPanel shows site URL, summary, and nsec for anonymous deploys. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: File input UI — DeployZone and FileTree components</name>
  <files>
    apps/spa/src/components/DeployZone.svelte,
    apps/spa/src/components/FileTree.svelte
  </files>
  <action>
1. **DeployZone.svelte** — File input area:
   - Large drag-drop zone with dashed border. Text: "Drop your site files here" with icon.
   - Below drop zone: two buttons side by side — "Choose Folder" and "Upload Archive (.zip, .tar.gz)".
   - Drag-drop: listen for dragenter, dragover, dragleave, drop events. On drop, read files from DataTransferItemList — if entry is directory (webkitGetAsEntry), recursively read. If files, check extension for archive.
   - "Choose Folder" calls pickDirectory() from files.js.
   - "Upload Archive" calls pickArchive() from files.js.
   - On file selection: run autoExcludeVCS(), then scanFiles() for warnings, then buildFileTree(). Dispatch 'files-selected' event with {files, tree, warnings, excluded} to App.svelte.
   - Tailwind: Dashed border `border-2 border-dashed border-slate-600 hover:border-purple-500`, large padding, centered text.

2. **FileTree.svelte** — Expandable file tree display:
   - Props: `tree` (nested tree from buildFileTree), `warnings` (from scanFiles), `onToggleExclude(path)`.
   - Recursive rendering: directories are collapsible (click to expand/collapse), files show name + human-readable size.
   - Warning icons: if a file's path is in warnings array, show a yellow/amber warning icon next to it. Click icon to see reason (tooltip or expandable detail). Checkbox next to warned files to exclude/include them.
   - Auto-excluded files (.git/ etc.): shown at top as a notice banner, not in tree.
   - Directory sizes: sum of children.
   - Tailwind: `text-sm font-mono`, indented levels, `bg-slate-800 rounded-lg p-4`.
  </action>
  <verify>
    <automated>cd apps/spa && npm run build</automated>
  </verify>
  <done>DeployZone handles drag-drop, folder picker, and archive upload. FileTree renders expandable directory structure with warning icons and exclude checkboxes. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: Auth UI, advanced config, tools resources + tools.test.js</name>
  <files>
    apps/spa/src/components/LoginModal.svelte,
    apps/spa/src/components/NIP46Dialog.svelte,
    apps/spa/src/components/AdvancedConfig.svelte,
    apps/spa/src/components/ToolsResources.svelte,
    apps/spa/src/lib/tools-resources.yaml,
    apps/spa/src/lib/__tests__/tools.test.js
  </files>
  <action>
1. **LoginModal.svelte** — Authentication choice modal:
   - Props: `show` (boolean), dispatches 'close' and 'login' events.
   - Modal overlay with dark backdrop.
   - Title: "Login with your Nostr identity"
   - Two option cards side by side:
     a. "Browser Extension" — icon + description "Use Alby, nos2x, or other NIP-07 extension". Click calls createExtensionSigner(). On success: fetchProfile() for avatar/name, update session store, dispatch 'login'. On ExtensionMissingError: show inline error "No Nostr extension found. Install Alby or nos2x."
     b. "Remote Signer" — icon + description "Connect with Amber, nsecBunker, or other NIP-46 signer". Click opens NIP46Dialog.
   - "Or deploy anonymously" text at bottom (dismisses modal).
   - Tailwind: `bg-slate-800 rounded-xl shadow-2xl`, max-w-md centered.

2. **NIP46Dialog.svelte** — NIP-46 remote signer connection:
   - On mount: immediately call createNostrConnectSigner(). Render QR code of the returned `nostrconnect://` URI using `qrcode` library (use QRCode.toCanvas or QRCode.toDataURL to render to img/canvas element).
   - Show URI text below QR with "Copy" button.
   - Divider: "--- or paste bunker URI ---"
   - Text input for bunker:// URI with "Connect" button. On submit: call connectFromBunkerURI().
   - While waiting: "Waiting for signer to connect..." with spinner animation.
   - On signer connected (waitForSigner resolves): fetch profile, update session, dispatch 'login' event.
   - Error handling: timeout message after 60s, invalid URI message.
   - QR code import: `import QRCode from 'qrcode'`. Use `QRCode.toCanvas(canvasElement, uri, {width: 256, margin: 2, color: {dark: '#e2e8f0', light: '#1e293b'}})` for dark-theme QR.
   - Tailwind: `bg-slate-800`, centered QR, mono font for URI.

3. **AdvancedConfig.svelte** — Collapsible server configuration:
   - Collapsed by default. Click header "Advanced Settings" with chevron to expand.
   - Two sections when expanded:
     a. "Additional Relays" — text input + "Add" button. List of added URLs with "Remove" button. Reads/writes serverConfig.extraRelays store.
     b. "Additional Blossom Servers" — same pattern for serverConfig.extraBlossoms.
   - Note text: "Your manifest will always be published to wss://nsite.run and blobs uploaded to nsite.run. These are additional servers."
   - Tailwind: `border border-slate-700 rounded-lg`, muted text colors.

4. **ToolsResources.svelte** — Educational links from YAML data:
   - Import tools-resources.yaml as data.
   - Render categorized sections: Gateways, Deploy Tools, Management Tools, Blossom Servers, Relays, Reference, Informational.
   - Each item: name (as link), description. Grid layout for cards.
   - Tailwind: section headers with `text-lg font-semibold`, cards with `bg-slate-800 rounded-lg p-4 hover:bg-slate-700 transition`, links in purple-400.

5. **tools-resources.yaml** — Copy from ~/Develop/nsite.run/src/lib/tools-resources.yaml. Update nsite.run entry to reference this project. Add nsite.run gateway to nsiteGateways list if not present. Structure must be:
   ```yaml
   categories:
     - name: "Category Name"
       items:
         - name: "Tool Name"
           url: "https://example.com"
           description: "Brief description"
   ```
   Each category has a `name` string and `items` array. Each item has `name`, `url`, and `description` strings.

6. **src/lib/__tests__/tools.test.js** — Tests for SPA-12 (tools-resources.yaml):
   ```js
   import { describe, it, expect } from 'vitest';
   import toolsData from '../tools-resources.yaml';

   describe('tools-resources.yaml', () => {
     it('loads without error and is an object', () => {
       expect(toolsData).toBeDefined();
       expect(typeof toolsData).toBe('object');
     });

     it('has a categories array with at least 3 categories', () => {
       expect(Array.isArray(toolsData.categories)).toBe(true);
       expect(toolsData.categories.length).toBeGreaterThanOrEqual(3);
     });

     it('every category has a name string and items array', () => {
       for (const cat of toolsData.categories) {
         expect(typeof cat.name).toBe('string');
         expect(cat.name.length).toBeGreaterThan(0);
         expect(Array.isArray(cat.items)).toBe(true);
         expect(cat.items.length).toBeGreaterThan(0);
       }
     });

     it('every item has name, url, and description strings', () => {
       for (const cat of toolsData.categories) {
         for (const item of cat.items) {
           expect(typeof item.name).toBe('string');
           expect(typeof item.url).toBe('string');
           expect(item.url).toMatch(/^https?:\/\//);
           expect(typeof item.description).toBe('string');
         }
       }
     });

     it('includes nsite.run gateway entry', () => {
       const allItems = toolsData.categories.flatMap(c => c.items);
       const nsiteEntry = allItems.find(i => i.url.includes('nsite.run'));
       expect(nsiteEntry).toBeDefined();
     });
   });
   ```
   Note: This test file imports the YAML via Vite's yaml plugin, so it needs the vitest config to include the yaml plugin. Update `vitest.config.js` (or the test section in `vite.config.js`) to include the yaml plugin if not already configured. The simplest approach: change vitest.config.js to import from vite.config.js or duplicate the yaml plugin config.
  </action>
  <verify>
    <automated>cd apps/spa && npx vitest run src/lib/__tests__/tools.test.js && npm run build</automated>
  </verify>
  <done>Login modal with Extension and Remote Signer options works. NIP-46 dialog shows QR code. Advanced config collapses/expands. ToolsResources renders YAML data. tools.test.js validates YAML structure and content. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `cd apps/spa && npm run build` — build succeeds with all components
2. `cd apps/spa && npx vitest run src/lib/__tests__/tools.test.js` — tools YAML tests pass
3. `cd apps/spa && npm test` — full suite passes (no regressions)
4. All 10 Svelte component files exist in src/components/
5. tools-resources.yaml imported and rendered
</verification>

<success_criteria>
- Full deploy flow UI: idle -> file selection -> review -> progress -> success
- Login modal with NIP-07 and NIP-46 options
- NIP-46 QR code renders immediately
- File tree shows warnings with exclude checkboxes
- SPA fallback checkbox present
- Educational content visible below deploy zone
- Tools & Resources section with categorized links
- tools-resources.yaml passes structural validation tests
- Dark theme throughout
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/06-spa-deploy-interface/06-04-SUMMARY.md`
</output>
