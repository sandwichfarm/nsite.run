---
phase: 11-spa-named-site-support
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/spa/src/lib/base36.js
  - apps/spa/src/lib/publish.js
  - apps/spa/src/lib/nostr.js
  - apps/spa/src/lib/__tests__/publish.test.js
autonomous: true
requirements: [SPA-16, SPA-17, SPA-18]

must_haves:
  truths:
    - "buildManifest can produce kind 35128 events with a d tag for named sites"
    - "buildManifest can produce kind 15128 events with title and description tags"
    - "publishEmptyManifest can publish kind 35128 empty manifests with a d tag for named site deletion"
    - "fetchExistingManifest returns both root (15128) and named (35128) manifests for a pubkey"
    - "base36Encode is available as a JS module for generating named site URLs in the SPA"
  artifacts:
    - path: "apps/spa/src/lib/base36.js"
      provides: "base36Encode function (JS port from shared package)"
      exports: ["base36Encode"]
    - path: "apps/spa/src/lib/publish.js"
      provides: "Updated buildManifest, publishManifest, publishEmptyManifest with kind/dTag/title/description params"
      exports: ["buildManifest", "publishManifest", "publishEmptyManifest"]
    - path: "apps/spa/src/lib/nostr.js"
      provides: "Updated fetchExistingManifest returning root + named manifests, new fetchAllManifests function"
      exports: ["fetchExistingManifest", "fetchAllManifests"]
    - path: "apps/spa/src/lib/__tests__/publish.test.js"
      provides: "Tests for new buildManifest params (kind, dTag, title, description)"
  key_links:
    - from: "apps/spa/src/lib/publish.js"
      to: "buildManifest callers"
      via: "options object parameter"
      pattern: "buildManifest\\(files, servers, \\{"
    - from: "apps/spa/src/lib/nostr.js"
      to: "apps/spa/src/App.svelte"
      via: "fetchAllManifests returns { root, named[] }"
      pattern: "fetchAllManifests"
---

<objective>
Add named site support to the SPA's data layer: update manifest building to support kind 35128 with dTag, add title/description metadata tags, update manifest fetching to retrieve both root and named sites, and port base36Encode for URL generation.

Purpose: The UI layer (Plan 02) needs these contracts to wire up the deploy flow and manage tab. This plan establishes the function signatures and data shapes first.
Output: Updated publish.js, nostr.js, new base36.js, updated tests
</objective>

<execution_context>
@/home/sandwich/.claude/get-shit-done/workflows/execute-plan.md
@/home/sandwich/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-spa-named-site-support/11-CONTEXT.md
@.planning/phases/10-gateway-named-site-encoding/10-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From apps/spa/src/lib/publish.js (CURRENT signatures to update):
```javascript
export function buildManifest(files, servers, spaFallback) { ... }
// Returns: { kind: 15128, created_at, tags: [...pathTags, ...serverTags, ['client', 'nsite.run']], content: '' }

export async function publishManifest(signer, files, servers, relays, spaFallback) { ... }
// Calls buildManifest, signs, publishes to all relays

export async function publishEmptyManifest(signer, relays) { ... }
// Publishes empty kind 15128 manifest for site deletion
```

From apps/spa/src/lib/nostr.js (CURRENT signature to update):
```javascript
export async function fetchExistingManifest(pubkey, relays) { ... }
// Queries kind 15128 only, returns single newest event or null

export function queryRelay(url, filter) { ... }
// Opens WebSocket, sends REQ, collects events until EOSE
```

From packages/shared/src/base36.ts (to port to JS):
```typescript
export function base36Encode(bytes: Uint8Array): string { ... }
// Converts 32-byte Uint8Array to 50-char lowercase base36 string
```

From apps/spa/src/lib/__tests__/publish.test.js (test pattern):
```javascript
import { describe, it, expect } from 'vitest';
import { buildManifest } from '../publish.js';
// Tests use describe/it/expect from vitest
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update publish.js for named sites + metadata, update tests</name>
  <files>apps/spa/src/lib/publish.js, apps/spa/src/lib/__tests__/publish.test.js</files>
  <read_first>
    apps/spa/src/lib/publish.js
    apps/spa/src/lib/__tests__/publish.test.js
    .planning/phases/11-spa-named-site-support/11-CONTEXT.md
  </read_first>
  <behavior>
    - buildManifest with no options (backward compat): kind=15128, no d/title/description tags
    - buildManifest with {kind: 35128, dTag: 'blog'}: kind=35128, has ['d','blog'] tag
    - buildManifest with {title: 'My Site'}: has ['title','My Site'] tag
    - buildManifest with {description: 'A blog'}: has ['description','A blog'] tag
    - buildManifest with {title: '', description: ''}: no title or description tags (empty strings omitted)
    - buildManifest with all options combined: kind=35128 + d tag + title + description + path + server + client tags
    - publishEmptyManifest with no dTag: publishes kind 15128 (backward compat)
    - publishEmptyManifest with dTag 'blog': publishes kind 35128 with ['d','blog'] tag
  </behavior>
  <action>
    1. UPDATE `buildManifest` signature from `buildManifest(files, servers, spaFallback)` to `buildManifest(files, servers, options)` where options is `{ spaFallback, kind, dTag, title, description }`. Default kind to 15128.
       - For backward compat: if `options` is a boolean, treat it as `{ spaFallback: options }` (handles existing callers during transition).
       - Add `['d', dTag]` tag when `dTag` is truthy.
       - Add `['title', title]` tag when `title` is a non-empty string.
       - Add `['description', description]` tag when `description` is a non-empty string.
       - Set event kind from `options.kind || 15128`.

    2. UPDATE `publishManifest` signature from `publishManifest(signer, files, servers, relays, spaFallback)` to `publishManifest(signer, files, servers, relays, options)` where options matches buildManifest's options object. Pass options through to buildManifest.

    3. UPDATE `publishEmptyManifest` signature from `publishEmptyManifest(signer, relays)` to `publishEmptyManifest(signer, relays, options)` where options is `{ dTag }`. Optional param, defaults to empty.
       - When dTag provided: set kind to 35128, add `['d', dTag]` tag.
       - When no dTag: keep kind 15128 (backward compat).

    4. UPDATE existing tests to use new options object format where spaFallback was boolean.

    5. ADD new tests for:
       - kind 35128 with dTag
       - title tag inclusion
       - description tag inclusion
       - empty title/description omission
       - publishEmptyManifest backward compat (no options)
       - publishEmptyManifest with dTag
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nsite.run/apps/spa && npx vitest --run src/lib/__tests__/publish.test.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "kind.*35128" apps/spa/src/lib/publish.js (kind 35128 support exists)
    - grep -q "'d'" apps/spa/src/lib/publish.js (d tag added for named sites)
    - grep -q "'title'" apps/spa/src/lib/publish.js (title tag support exists)
    - grep -q "'description'" apps/spa/src/lib/publish.js (description tag support exists)
    - grep -q "kind 35128" apps/spa/src/lib/__tests__/publish.test.js (tests for named site kind)
    - grep -q "title" apps/spa/src/lib/__tests__/publish.test.js (tests for title tag)
  </acceptance_criteria>
  <done>buildManifest produces kind 35128 events with d tag when options.kind=35128 and options.dTag set; title/description tags added when non-empty; publishEmptyManifest supports optional dTag for named site deletion; all existing tests still pass with backward-compat boolean handling; new tests cover all named site and metadata scenarios</done>
</task>

<task type="auto">
  <name>Task 2: Add base36.js utility and update nostr.js for multi-manifest fetch</name>
  <files>apps/spa/src/lib/base36.js, apps/spa/src/lib/nostr.js</files>
  <read_first>
    packages/shared/src/base36.ts
    apps/spa/src/lib/nostr.js
    .planning/phases/11-spa-named-site-support/11-CONTEXT.md
  </read_first>
  <action>
    1. CREATE `apps/spa/src/lib/base36.js` — a JS port of the `base36Encode` function from `packages/shared/src/base36.ts`.
       - Copy the algorithm exactly: BigInt divmod loop, 50-char zero-padded output, `0123456789abcdefghijklmnopqrstuvwxyz` alphabet.
       - Export only `base36Encode(bytes)` — decode is not needed in the SPA.
       - Add JSDoc with `@param {Uint8Array} bytes` and `@returns {string}`.

    2. CREATE `fetchAllManifests(pubkey, relays)` function in `nostr.js`:
       - Query relays with TWO filters in parallel:
         - Filter 1: `{ kinds: [15128], authors: [pubkey], limit: 1 }` (root manifest)
         - Filter 2: `{ kinds: [35128], authors: [pubkey] }` (ALL named manifests — no limit, no #d filter)
       - Use the existing `queryRelay` function and `Promise.allSettled` pattern.
       - Deduplicate events by `event.id`.
       - For root (kind 15128): return the most recent event (same as current fetchExistingManifest logic). Filter out empty manifests (0 path tags).
       - For named (kind 35128): group by d-tag value, keep only the most recent event per d-tag. Filter out empty manifests (0 path tags).
       - Return `{ root: event|null, named: event[] }` where named is an array sorted by created_at descending.

    3. KEEP existing `fetchExistingManifest` function UNCHANGED for backward compat — it's still used by App.svelte in the current code and will be replaced by callers in Plan 02.

    4. Add helper function `getManifestDTag(event)` that extracts the d-tag value from a manifest event's tags array: `event.tags.find(t => t[0] === 'd')?.[1] || null`. Export it.

    5. Add helper function `getManifestTitle(event)` that extracts `event.tags.find(t => t[0] === 'title')?.[1] || ''`. Export it.

    6. Add helper function `getManifestDescription(event)` that extracts `event.tags.find(t => t[0] === 'description')?.[1] || ''`. Export it.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nsite.run/apps/spa && node -e "import('./src/lib/base36.js').then(m => { const bytes = new Uint8Array(32); bytes[31] = 1; const r = m.base36Encode(bytes); console.log('encode result:', r, 'length:', r.length, 'ok:', r.length === 50); if (r.length !== 50) process.exit(1); })"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "base36Encode" apps/spa/src/lib/base36.js (base36 encode function exists)
    - grep -q "36n" apps/spa/src/lib/base36.js (uses BigInt base36 math)
    - grep -q "padStart.*50" apps/spa/src/lib/base36.js (pads to 50 chars)
    - grep -q "fetchAllManifests" apps/spa/src/lib/nostr.js (new multi-manifest function exists)
    - grep -q "35128" apps/spa/src/lib/nostr.js (kind 35128 query in nostr.js)
    - grep -q "getManifestDTag" apps/spa/src/lib/nostr.js (d-tag helper exists)
    - grep -q "getManifestTitle" apps/spa/src/lib/nostr.js (title helper exists)
    - grep -q "getManifestDescription" apps/spa/src/lib/nostr.js (description helper exists)
  </acceptance_criteria>
  <done>base36Encode available in SPA lib (JS port producing 50-char output from 32-byte input); fetchAllManifests queries both kind 15128 and 35128, returns { root, named[] } with dedup and empty-manifest filtering; helper functions extract dTag, title, description from manifest events</done>
</task>

</tasks>

<verification>
1. `cd apps/spa && npx vitest --run` — all existing + new tests pass
2. `node -e "import('./apps/spa/src/lib/base36.js').then(m => console.log(m.base36Encode(new Uint8Array(32)).length))"` — prints 50
3. grep confirms all new exports exist in the modified files
</verification>

<success_criteria>
- buildManifest supports kind 35128 + dTag + title + description via options object
- publishManifest passes options through to buildManifest
- publishEmptyManifest accepts optional dTag for named site deletion
- fetchAllManifests returns { root, named[] } querying both kinds
- base36Encode available as JS module
- All tests pass including new ones for named site manifest building
</success_criteria>

<output>
After completion, create `.planning/phases/11-spa-named-site-support/11-01-SUMMARY.md`
</output>
